# RAG Agent (Node/TypeScript)

TypeScript app for the RAG AI agent. Run from this directory.

## Setup

```bash
npm install
cp ../.env.example .env   # then edit .env with your keys
```

## Run

```bash
npm start
```

Build (compile to `dist/`):

```bash
npm run build
node dist/index.js
```
